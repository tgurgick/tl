# Deprecated — use _templates/story-folder/ instead

Stories are now folders, not flat files. Copy `_templates/story-folder/` to `3-stories/<story-slug>/` when creating a new story. See `README.md` and `1-research/2026-04-06-story-as-skill-architecture.md` for why.
